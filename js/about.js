/**
 * about.js — About page specific interactions.
 */
document.addEventListener('DOMContentLoaded', () => {
    // Subtle entry animation for bio paragraphs if needed
    console.log('About page loaded');
});
